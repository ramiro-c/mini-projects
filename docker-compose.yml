# Root docker-compose — shared network and base services.
# Each project under projects/ has its own docker-compose that extends this.

name: mini-projects

networks:
  backend:
    driver: bridge

volumes:
  sqldata:
